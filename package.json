{
  "name": "hevno-engine-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:plugins": "npm run build --workspaces"
  },
  "devDependencies": {
    "vite": "^5.2.11"
  }
}